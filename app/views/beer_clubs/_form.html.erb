  <%= form_with(model: beer_club) do |form| %>
    <% if beer_club.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

        <ul>
          <% beer_club.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

  <div class="form-group">
    <%= form.label :name, "Name:", class: "col-md-4 control-label" %>
    <div class="col-md-8">
      <%= form.text_field :name, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= form.label :founded, "Founded", class: "col-md-4 control-label" %>
    <div class="col-md-8">
      <%= form.text_field :founded, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= form.label :city, "City", class: "col-md-4 control-label" %>
    <div class="col-md-8">
      <%= form.text_field :city, class: "form-control" %>
    </div>
  </div>
  <br>
  <%= form.submit "Submit", class: "btn btn-default btn-primary" %> 
<% end %>
