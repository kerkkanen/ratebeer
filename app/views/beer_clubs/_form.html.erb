  <% if beer_club.errors.any? %>"
    <div style="color: red">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
        <% beer_club.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %> 
 
  <%= form_with(model: beer_club) do |form| %>
  <form>
  <div class="mb-3">
    <label class="form-label">Name</label>
    <input type="text" class="form-control" id=<%= form.text_field :name %>
  </div>
  <div class="mb-3">
    <label class="form-label">Founded</label>
    <input type="number" class="form-control" id=<%= form.number_field :founded %>
  </div>
   <div class="mb-3">
    <label class="form-label">City</label>
    <input type="text" class="form-control" id=<%= form.text_field :city %>
  </div>
  <button class="btn btn-primary", type=<%= form.submit %>Submit</button>
</form>
<% end %>
