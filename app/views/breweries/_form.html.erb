<%= form_with(model: brewery) do |form| %>
  <% if brewery.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(brewery.errors.count, "error") %> prohibited this brewery from being saved:</h2>

      <ul>
        <% brewery.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <form>
    <div class="mb-3">
      <label class="form-label">Name</label>
      <input type="text" class="form-control" id=<%= form.text_field :name %>
    </div>
    <div class="mb-3">
      <label class="form-label">Year</label>
      <input type="numeral" class="form-control" id=<%= form.number_field :year %>
    </div>
    <div>
      <%= form.label :active, style: "display: block" %>
      <%= form.check_box :active %>
    </div>
    <button class="btn btn-primary", type=<%= form.submit %>Submit</button>
  </form>
<% end %>
